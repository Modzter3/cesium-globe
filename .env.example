# Copy to .env.local (never commit .env.local).
# Rotate any key that was pasted into chat or committed by mistake.
#
# Use the exact name FLIGHTAWARE_API_KEY (no spaces around =).
# Quotes are optional; if you use them, use straight " not smart quotes.

FLIGHTAWARE_API_KEY=
